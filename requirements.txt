# Runtime used by the taxi-demand Spark pipeline.
# Keep the versions aligned across the three team members and Docker image.
pyspark==3.5.1
pandas==2.3.3
matplotlib==3.10.8
pyarrow==23.0.1
pytest==8.4.2
